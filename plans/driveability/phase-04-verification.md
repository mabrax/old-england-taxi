# Phase 04 — Qualification record

> **Desktop priority amendment — 2026-09-08:** The user has prioritized desktop web driving and authorized the [repeatable desktop benchmark](./desktop-benchmark/README.md). iPad Safari session, interaction, performance and accessibility acceptance is **deferred, not passed**; preserve the existing touch behavior and all historical evidence. **Current desktop matrix, explicitly selected by the user in this continuation: Linux Chrome, Linux Firefox, and macOS Microsoft Edge. Desktop Safari is removed from acceptance scope; earlier Safari requirements below are historical and superseded.** Chrome remains the first profiling target. The user explicitly deferred macOS Edge testing and limited this continuation to Linux Chrome/Firefox; Linux evidence does not qualify the deferred Mac target. See the [desktop continuation](./desktop-qualification/README.md). Fixed desktop budgets and unresolved sustained/lifecycle/memory gates remain in force.

**Current continuation:** [Linux desktop qualification](./desktop-qualification/verification.md) now supplies valid Chrome/Firefox sustained and displayed lifecycle sessions, with retained incomplete attempts and the unexplained RAF-stall disposition and final review. That new evidence awaits champion review. The rest of this record retains the original Phase 04 observations and review.

**Historical review status: available-environment evidence independently reviewed; full qualification and stage exit remain open.** The parent champion reviewed the isolated implementation and evidence, including the sustained-acceptance correction. Actual desktop Safari and iPad Safari acceptance, sustained foreground-run validity, displayed-browser lifecycle and complete device-memory evidence remain required. The user authorized Phase 04 despite outstanding Phase 03 device checks; that authorization is not a test result.

Baseline: reviewed local `5b12c38`, on foundation `712fe6f`, isolated branch `codex/driveability-qualification` in `/home/mabrax/.codex/worktrees/63c8/old-england-taxi`. The [prospective plan/budgets](./phase-04-qualification.md#prospective-budgets-fixed-before-acceptance-runs) were committed as `d6dd37f` before acceptance runs. The application remains web-only. Canonical main/preview/cache and existing worktrees are protected; no push, merge or deployment is authorized or performed.

## Reproducible evidence

The [remaining-device and reproduction procedure](./phase-04-device-procedure.md) gives commands and operator checks. The [physical matrix](./phase-04-evidence/physics.json), [source queries](./phase-04-evidence/source-queries.json), [protected identities](./phase-04-evidence/identities.json), [accelerated Chromium run](./phase-04-evidence/chromium-headed.json), and [completed Chromium memory/history run](./phase-04-evidence/chromium-memory.json) identify artifacts, starts, commands, contact/query samples, actual engine/device configuration, performance percentiles, run errors and lifecycle counters. Final production asset hashes and protected-product identities are recorded with those results. Local screenshots/video paths and hashes identify visual evidence; binary media is not a prepared product and is not published.

Historical Phase 01–03 evidence informed the work but is not counted as a new pass. The independent Phase 03 review applies to `3dc7acab2ec18fc44e8d0fe2fd9eb0133f8be927`; it did not establish Phase 04 performance or iPad support.

## Physical operating envelope

Unchanged Rapier **0.20.0**, fixed **1/60 s**, maximum **five catch-up steps**, gravity −9.81 m/s². The vehicle remains a fully rotating 1100 kg, 2.0 × 0.9 × 4.4 m chassis with four ray-cast wheels, CCD, four extra solver iterations, 5 mm skin and eight CCD substeps. Forward limit **8 m/s**, reverse **3 m/s**; no new handling/physics tuning. The complete settings are embedded in the physical JSON and described in [Phase 02](./phase-02-verification.md#runtime-and-vehicle-decisions).

Acceptance keeps **≤3 cm** included-wall/ground penetration, **<4 cm** conservative boundary-envelope overhang, speed tolerance **0.01 m/s**, and no occupied-building centre frames or unintended recovery. Maximum-speed stops must be **<5 m and ≤1.2 s**. The 0.5 m escaped-envelope recovery guard is unchanged and is not a collision allowance. Pavement render lift remains presentation only.

Normal starts use the same bounded generic pavement/clearance search, complete chassis/wheel/suspension envelope, 6 m forward/3 m reverse launch corridor and 10 m boundary margin. No stored source-way ID, place name or coordinate enters runtime spawn logic. The street names below locate **test areas**, not legal routes or a promise to traverse each street.

| Cell / normal area | Artifact SHA-256 | Generic start X, Z (m) / candidate | Contact peak (mm) |
| --- | --- | --- | --- |
| Chicago River North grid / West Kinzie Street | `4a9037e4db68a6cd55cd3db90a0aa50988b550ca1f3cd7d68125d4a35f6473b6` | -83.771, 217.488 / 6 | 4.510 |
| Cambridge centre / Malcolm Street | `6dea8e06c7a3f40ac306b07236dcbd3779ba8cf4e87caaa3e490069217760cd4` | 152.417, -318.105 / 2 | 4.562 |
| London Tower Bridge / Tower Bridge Approach | `5f4887e391bafe1ded5e82519e42ceaaed35998655810b61b1f4019571cfb00d` | 85.470, -255.527 / 5 | 4.524 |
| Lucca historic centre / Corso Giuseppe Garibaldi | `8eff07f47eba43f30f5084232c0f4bfd086f6cc740660ad9435f6493c044e3cf` | -318.195, 319.003 / 1 | 4.762 |
| Trafalgar Square, London / The Mall | `3636dcb2c69face98dea581333c9309ed5c021364a0b953074b1a98c19e2fc6a` | -448.628, 413.041 / 1 | 4.495 |

All five generic starts newly pass settle → 120 steps forward → 60 coast → 120 brake → 120 reverse → 120 brake → reset. At the sample endpoints, forward speed is about **5.813 m/s**, coasting **5.157 m/s**, braking below **0.001 m/s**, reverse about **−2.993 m/s**. Four wheels remain supported and there are no occupied-building centres or recoveries in those sequences. Reset clears motion and rejects held non-neutral input until rearmed.

**Contrasting normal maneuvers:** Cambridge/Malcolm Street and Chicago/West Kinzie Street start generically, settle 120 steps, accelerate 60, turn left with throttle 90, hold left + brake 120, reverse left for 160 (Cambridge) or 190 (Chicago), then brake left 120. Cambridge records 158 off-pavement-centre steps and at least three supported wheels during transient contact; Chicago records 286 and four wheels. Both return with the full inflated vehicle rectangle on pavement, stopped, with no reset/recovery or occupied-building centre. These area-specific command durations are evidence scripts, never runtime exceptions. Real keyboard/browser sessions additionally verify rendered steering, Stop, brake/reverse and recovery controls.

**Moving and stationary contacts:** every prepared cell has mesh-derived clear test poses, seeded 8 m/s head/glancing/corner approaches and recorded 240-step outcomes. These controlled placements are separate from normal starts. Head-on contact is followed by 120 brake steps and 600 stationary steps, then explicit stuck reset. Maximum sampled included-building penetration is **4.762 mm**. Tower Bridge's first corner candidate meets the southern artificial boundary before the building. That attempt remains recorded; the next clear mesh-derived candidate provides 190 building-contact steps with no boundary contact. Each selected prepared head/glancing/corner case explicitly records building contact (147–190 steps), and all five stationary runs retain building contact for all 600 steps. Controlled closed-building and four-boundary fixtures cover head/glancing/corner/reverse and all limit faces independently; boundary overhang peaks at **2.304 mm**, without recovery.

**Queries and rare failures:** occupied points under real roof triangles pass in all five cells. Real 1 m clearance boxes in Cambridge relation **3240**, Lucca relation **2317787**, and Trafalgar relation **78595** return clear courtyard/no building surface intersection. These are queries, not claimed courtyard vehicle entrances. Chicago and Tower Bridge have no included courtyard holes; controlled concave and closed-courtyard geometry supplies their shared-runtime query regression. An unsafe graph-hint fixture disables driving and leaves inspection. Controlled overturned placement permits manual upright reset; escaped, fallen and nonfinite placements recover within one fixed step and disarm input. Session tests verify that recovery pauses the integrated session.

**Cadence and stops:** 30/60/120/144 Hz session replays produce equal physical endpoints for straight motion, both off-road maneuvers, a seeded head contact/brake/reverse sequence and an east-boundary sequence. Fixed-step counts and safe endpoints are asserted in the raw record. Existing stalled-frame and actual hidden-tab tests verify bounded catch-up and explicit resume. From approximately +7.983 m/s, stopping takes **1.0 s / 3.800 m**; from −2.993 m/s, **0.4 s / 0.547 m**, within the unchanged limits.

## Bounded fixes and retained failures

1. **Contact measurement correction, with no physics change.** Initial all-cell evidence reported **0.146779 m** penetration at Tower Bridge's near-boundary corner. At worst step 238, the fresh cuboid query returned a slightly tilted normal and witness points about 80 m apart along the long boundary. The chassis maximum Z was approximately **373.981 m**, while the wall began at **373.986 m**: it remained about 5 mm inside. The raw query was not a reliable penetration depth. Evidence now computes cuboid overlap independently using all 15 OBB separating axes, preserving the raw shape-distance maximum alongside it; included triangle-mesh contacts still use solver/fresh contacts and occupied-centre checks. Regression tests cover known separated/shallow/deep box cases and the actual Tower Bridge approach at 3/5/8 m/s with distance variants. The [failed initial record](./phase-04-evidence/physics-initial-failed.json) and [exploratory solver/CCD/skin observations](./phase-04-evidence/contact-query-diagnosis.json) are retained. No source limitation is used to dismiss a physics failure, and no explored physics-setting change was adopted.
2. **Navigation/loading-owner cleanup.** Initial Chromium history cycles grew from **66,047,646 bytes** at cycle 5 to **90,215,112** at cycle 20 after GC, exceeding the unchanged **20,971,520-byte** growth allowance. Browser document counts plateaued at 14; active scene counters stayed at one world. The scene freed physics/GPU resources, but cached Svelte/loading ownership could retain its disposed controller and artifact. `SceneViewport` now aborts the load, disposes the controller and releases its reference on `pagehide` as well as unmount. A shared module-level restore callback does not capture the old scene and reloads a cached disposed page once. This also prevents late artifact loading from constructing a new scene in a hidden navigated page. Final history/memory reruns below evaluate the correction.
3. **Harness setup corrections, not application passes.** Firefox rejected reserved port 4190; preview moved to 4191. The initial Puppeteer locator closed over an unavailable label, and immediate reset reads could precede the next rendered dataset update; the final harness matches accessible button text, clicks its rendered coordinates and waits two render frames. An aria-hidden arrow initially prevented a Firefox generation-button match; the final accessible-text locator passed. Initial source-query tooling omitted `await` and assumed ring-role metadata; it now reads the actual offline API and ring ordering. An initial full test run found that the scene mock lacked `window.location` for opt-in timing; its browser mock was updated. Failed attempts/logs remain distinguished from final results. A displayed Chromium run recorded all cold/functional/sustained scenarios, then timed out during ElementHandle evaluation in lifecycle cycle 4. Independent review subsequently identified an unexpected pause inside its sustained Trafalgar scenario; that separate protocol gap is detailed below. A coordinate-based retry completed 12 cycles before the app entered paused mode during the next drive (zero recoveries, intact world); it is not a completed lifecycle pass. These interruptions are retained, without inferring a device/browser cause from the timeout alone. Headless lifecycle qualification is recorded separately. The recording harness initially selected the old page created before recording; a unique active-page URL fixes CDP selection. A later recording process received SIGTERM after the keyboard cases, and the orbit capture timed out after four frames during concurrent functional work; these partial attempts are retained separately from serial reruns. Visual inspection then found recorded portrait/phone captures at the previous viewport size; the final harness sets CDP device metrics on the active input target and asserts exact inner dimensions. Those mislabeled captures are excluded from viewport acceptance.

## Browser and performance acceptance

Engineering budgets are fixed in `d6dd37f`. They are authorized implementation choices, not numbers specified by the user. Startup is measured on loopback with fresh browser contexts, no artificial network throttle and generation API deliberately unavailable. First browser-process visits include initialization; later fresh contexts can benefit from OS/shader/JIT caches. These are not mobile-network download guarantees.

Host: **Fedora Linux 44**, AMD **Ryzen 7 5700X3D**, physical **NVIDIA RTX 4070 Ti SUPER**, driver **610.57.04**. Desktop viewport **1440×900, DPR 1**. No native application is built. Engine versions, reported backend and exact results are in the table/raw records below. Firefox's renderer string masks the hardware model as “NVIDIA GeForce GTX 980, or similar”; it is not a second physical GPU claim.

| Configuration / final code | Cold setup / physics init / ready maximum (ms), 15 visits | Sustained frame interval p95 / p99 (ms) | Step p95 / p99 (ms) | Frame CPU p95 (ms) | Outcome |
| --- | --- | --- | --- | --- | --- |
| Chromium 153.0.8010.12, displayed, ANGLE/OpenGL ES 3.2 on RTX 4070 Ti SUPER | 159.10 / 397.70 / 822.69 | 16.80 / 16.90 | 0.20 / 0.20 | 0.30 | Cold passes; numeric sustained targets met. Trafalgar foreground acceptance indeterminate; legacy interval coverage incomplete |
| Firefox 155.0, headless, NVIDIA WebGL 2.0 (masked renderer string) | 196.00 / 337.00 / 967.00 | 17.08 / 17.08 | 1.00 / 1.00 | 1.00 | Cold passes; numeric sustained targets met. No invalid pre-reset endpoint observed; legacy interval coverage incomplete |

Each row reports the worst percentile across the four individually recorded scenarios; they are not pooled distributions. The [Firefox raw record](./phase-04-evidence/firefox.json) and [Chromium raw record](./phase-04-evidence/chromium-headed.json) retain all scenario samples/endpoints. Both have no unintended recovery. All 15 cold visits in each final engine run pass the 1000/3000/5000 ms setup/init/usable-page targets.

**Independent-review correction — sustained acceptance:** the [review of the original records](./phase-04-evidence/sustained-protocol-review.json) supersedes the earlier all-four-sustained-pass wording and the legacy sustained acceptance booleans, including their summary in `checks-and-media.json`. Original records and numeric thresholds are unchanged. The table reports measured numeric results, not proof of a complete valid foreground-driving protocol.

In displayed Chromium Trafalgar, `sustained[3].actions[27]` is already **paused before the cycle-28 scripted reset**, at elapsed **121945.770028 ms**, speed **−1.5466121565493562 m/s**, **7295** steps, **27** resets and **zero** recoveries, with `hidden:false` and the canvas focused. The last sampled pose is also paused at the same step/speed. Although the endpoint is after the requested 120 s, the pause onset and duration were not recorded, and pose/action timestamps use different clocks. The original data cannot establish whether the required interval was continuously valid outside deliberate resets. **Trafalgar sustained foreground acceptance is incomplete/indeterminate**, despite passing numeric percentiles. Do not truncate away the sample, count a later reset as recovery of the protocol, or infer an application defect/cause from this observation.

The audit inspected all **12 legacy sustained rows** (displayed Chromium, Firefox and initial SwiftShader). The other 11 have no invalid pre-reset action endpoint; their sparse non-driving pose samples lack reset-phase markers and cannot all be classified as unexpected. Their numeric results remain as recorded, including the SwiftShader failures. Complete interval validity is **not retrospectively established** for those rows either; absence of a bad endpoint is not new foreground-protocol acceptance. Cold visits, the physical matrix, functional checks and separate lifecycle/memory results keep their existing scope.

The runner now records drive/reset state intervals, visibility/focus events, mutation changes and command checkpoints, with a 100 ms watchdog independent of RAF. It checks the driving endpoint **before** opening a deliberate reset window, verifies resumed state, and requires physics steps to advance during each held command. A read-only phase getter labels sampled poses. Unexpected paused/hidden/focus-lost intervals, incomplete runs and evidence overflow latch a rejection; a later reset cannot clear it. Mutation history catches a pause/resume within one task. `numericBudgets`, protocol/correctness results and overall acceptance are separate, and partial evidence is saved before closing a failed run. Observation timestamps bound state changes; they do not claim exact event onset.

Validation for this correction: **eight deterministic guard tests** ([log](./phase-04-evidence/protocol-guard-tests.log)) and **four real Chromium DOM-fixture checks** ([record](./phase-04-evidence/protocol-guard-browser.json)) pass, covering deliberate resets, paused endpoints, transient mutation batches, actual hidden-tab return, watchdog observation, unfinished/no-progress cases and bounded-log overflow. These are harness tests, not application performance reruns. No application matrix was repeated and no new sustained performance pass is claimed; runtime code and qualification targets are unchanged.

**Final Chromium memory/history:** a separate headless SwiftShader run completes **20 cycles / 60 resets**, with at least 161 fixed steps per cycle. Cycle-5 JS heap is **37,512,695 bytes** and cycle-20 **47,541,670**; growth **10,028,975** is below **20,971,520** and every sample is below 256 MiB. All cycles have one canvas, body and controller, seven colliders, and 20 Three geometries / zero textures / four programs. Browser Back reports navigation type `reload` and creates zero-step, zero-reset neutral inspection. This rerun passes the unchanged JS-heap budget. The later harness also replaced ElementHandles with coordinate clicks, so the heap difference is not an isolated causal measurement of the production fix. Source ownership, navigation tests and the late-load browser check substantiate the cleanup change; no native/WASM/GPU memory measurement is claimed.

**Displayed Chromium lifecycle remains incomplete:** one run stopped after three cycles on an automation protocol timeout; the next stopped after 12 cycles when driving paused. Their partial results are retained in [the interrupted run](./phase-04-evidence/chromium-headed-lifecycle-interrupted.json), and do not count as a 20-cycle pass. A third foreground/focus-instrumented attempt [timed out before its first rendered vehicle](./phase-04-evidence/chromium-headed-lifecycle-startup-timeout.json). A [startup diagnostic](./phase-04-evidence/headed-startup-raf-diagnostic.json) found ready physics in 456.8 ms and seven colliders, while an independent page RAF counter stopped at **one callback over three seconds**, with `hidden:false` and `hasFocus:true`; the app frame/resource snapshot also stayed at its initial value. The only HTTP error was `/favicon.ico` (404). This supports a browser/display scheduling stall, not an isolated application-loop failure; the underlying cause is unresolved. No timeout or RAF failure is reclassified as a pass. The full headless run is separate evidence. Firefox completes 20 lifecycle cycles and Back restoration with stable active counts, but its heap/total-memory acceptance is unmeasured.

The initial headless Chromium/SwiftShader run misses the frame-interval target. Cambridge p95/p99 **50/50.1 ms**, Lucca **49.9/50 ms**, Trafalgar **33.5/50 ms**; Chicago's raw p95 is **33.400000000001455 ms**, an exact-comparison failure at the 33.4 ms threshold (numerical boundary, retained as recorded). These raw failures are not replaced by the hardware run. Physics-step p95 was **0.2 ms** and frame CPU p95 **0.7 ms**, with no dropped catch-up time or recovery. Software rendering, ordinary host background processes, and brief source/static-tool work during this initial run limit attribution of frame latency. The accelerated/Firefox timing runs were scheduled separately from other browser/tests. Later functional regression suites used isolated headless sessions, sometimes concurrently; their startup times are not added to the performance table.

Frame CPU duration covers simulation, camera and submission, not asynchronous GPU completion. RAF interval includes scheduling/rendering stalls. Timer precision differs between engines (Firefox measurements can be quantized to milliseconds). The observer caps arrays at 50,000 samples and the app clears each performance entry; no mutable physics/input handle is exposed. Repeated maneuvers include explicit reset pauses, and raw records retain actual elapsed duration, sample counts, physical step counts, poses and commands. They do not establish uninterrupted long-route traversal.

JS heap after GC excludes native/WASM/GPU memory. Browser history may retain a bounded number of documents even after scene teardown. Stable active counts and heap plateau are useful ownership evidence, not total-device memory certification. Firefox lacks a comparable `performance.memory`/forced-GC interface in this run; mark it unmeasured. Actual Safari/iPad memory and performance remain open.

## Rendered interaction, lifecycle and regressions

The retained fresh production results include:

| Check | Completed evidence |
| --- | --- |
| [Prepared loader/QA](./phase-04-evidence/zone-browser.json) | All five cells at desktop and mobile viewport; generated/source visibility, counts, camera reset; missing ID, invalid schema and QA-hash failures |
| [Physical scene browser](./phase-04-evidence/physics-browser.json) | All five cells at desktop and mobile viewport; collision inspection, pause/resume, real tab change, service-independent preparation, lazy-engine failure and fresh-navigation recovery |
| [Vehicle development regression](./phase-04-evidence/vehicle-browser.json) | Cambridge/Chicago at desktop and mobile viewport; visible chassis/wheels, seven fixed-duration command exercises, repeated reset, QA/camera independence, unsafe spawn and actual tab pause |
| [Generation interaction](./phase-04-evidence/generation-browser.json) | Desktop and tablet portrait: pending build blocks driving; outage retains job/vehicle; reconnect/failure permits explicit resume; cancellation retains cell; success creates one fresh neutral inspection world |
| [Firefox interactions](./phase-04-evidence/firefox-interactions.json) | Eight additional checks: wall camera/overhead/reset, unsafe spawn, real tabs, UI focus/capture interruption, synthetic orientation, generation stubs, blocked module, navigation during pending artifact load and late response/Back restoration |

These agent-browser regressions use **Chromium 152.0.7977.82** with the `HeadlessChrome/152.0.0.0` user agent and SwiftShader; the timed desktop row uses the separate Chromium 153 binary. Firefox interactions use actual **Firefox 155.0**. Each record identifies its own scope; a viewport is not a physical device.

The [complete recorded Chromium interaction run](./phase-04-evidence/driving-browser.json) passes **11 observations**, with **1440×900** keyboard scenarios in Chicago/Cambridge and trusted multi-touch at verified **820×1180**, **1180×820**, **390×844** and **844×390** CSS dimensions, DPR 1. It covers independent release, drag-off, cancellation/capture loss, Stop and brake/reverse, held-input rearming, synthetic orientation pause, rear-wall camera retraction/overhead/reset, unavailable spawn, driven Cambridge off-road return, actual hidden-tab departure/return and field editing. The final tab fixture creates and activates a page in the input page's browser context, explicitly observes `document.hidden === true`, then verifies paused return without catch-up.

The [serial orbit regression](./phase-04-evidence/rendering.json) passes eight distinct rendered frames—overview, orbit, low angle, close, pan, overhead, far and reset—with no page exceptions, plus a retained camera-motion WebM. The recorded driving WebM is qualitative visual evidence; its encoding/capture cadence is not used to infer simulation or frame timing. Exact layout acceptance uses the asserted CSS dimensions and PNG captures.

Visual review inspected exact-dimension phone/tablet stills, selected frame sequences across the 80.6 s encoded driving video, all eight orbit frames, and full-frame Chromium/Firefox wall-camera and failure-state captures. The yellow vehicle stays visible through the reviewed steering/reverse/pause sequences; close-wall framing switches to overhead, and inspection resets return to the overview. Controls remain readable and separated in the tested layouts. This is sampled visual review, not a claim to have inspected every recorded frame.

Some post-resize CLI stills and recording frames cropped the bottom of the viewport even while the app reported 1440×900. Those crops are not framing acceptance. The final screenshot helper captures the explicit full viewport through CDP; a [separate camera rerun](./phase-04-evidence/camera-browser.json) passes camera, unavailable, off-road, real hidden-tab and editing checks with verified **1440×900** stills. The [media/check manifest](./phase-04-evidence/checks-and-media.json) records paths, hashes, dimensions, review scope and retained failed attempts. Binary media remains in this isolated worktree cache and is not committed or published.

Trusted keyboard/touch inputs exercise the actual rendered production app, with camera/video/frames inspected as well as state assertions. Linux Chromium tablet/phone layouts and synthetic orientation events are labelled emulation. Actual iPad multi-touch ergonomics, physical rotation, Safari chrome/virtual keyboard, assistive technology and on-device performance are still outstanding. Prepared driving is exercised with the generation API unavailable; separate delayed JSON stubs exercise generation gating, outage/reconnect/failure/cancellation/replacement without provider acquisition. Real local service/worker/cache behavior remains covered by the offline full suite.

`npm run verify` passes the offline five-cell corpus, **248 tests in 24 files**, Svelte/TypeScript checks with **zero errors and warnings**, and the production build. The [full log](./phase-04-evidence/verify.log) retains the existing Vite chunk-size warning: main JavaScript 607.50 kB and lazy vehicle/Rapier JavaScript 2,863.35 kB (gzip 164.74 / 1,082.39 kB). Those byte counts are not device-memory or frame-time evidence. Five new regressions exercise navigation ownership and independent cuboid-contact measurement. The final runtime build precedes the reported browser runs; subsequent edits affect tools, evidence and documentation only.

All **50 protected files** under the prepared public products, compiler tree and lockfile compare byte-for-byte with `5b12c38`. Runtime imports still use validated baked geometry and the lazily loaded physics engine; the compiler/source pipeline remains offline. No vehicle configuration, source processing, prepared artifact, package dependency or lockfile is changed.

## Geometry/source limits and stage exit

The source-query record identifies affected geometry and report decisions for every cell. Examples:

- **Chicago:** excluded building relation **9522023** has touching/intersecting rings; included bridge way **79657216** and tunnel/service way **24096761** are flattened. Open space at those exclusions or crossings is not real-world clearance evidence.
- **Cambridge:** included service/tunnel way **3993052** is flattened; footway **3986781** is excluded and crosses the source boundary. Courtyard relation **3240** has a passing query, with no claim of a modeled/drivable entrance through its walls.
- **Tower Bridge:** relation **6974419** is excluded for intersecting/touching rings; included trunk/bridge way **24951423** has no reconstructed layer. There are 29 bridge, 40 tunnel, 74 nonzero-layer and 368 building-part source features in the recorded report. Normal movement on Tower Bridge Approach does not certify bridge elevation, river support, missing parts or routes.
- **Lucca:** pedestrian/building-passage way **35164691** is excluded, and building part **368742667** is omitted. A clear query in relation **2317787** does not imply a passable modeled doorway.
- **Trafalgar:** service/building-passage way **4082561** is flattened and arch relation **17074208** has an unmodeled layer. Included solids may block a real passage that requires later geographic reconstruction. Relation **78595** supplies a passing courtyard query.

All cells retain flat support on unmodeled land/water, inferred widths, omitted barriers and conservative road-derived rectangular play limits. Full buildings/graph nodes may lie outside the acquisition bounds. The amber boundary is an artificial stopping limit. There are no legal-route, adjacency, all-street, native-app or geographic-repair claims.

**Full Phase 04 acceptance and Stage 03 exit remain open.** The independent review below accepts the bounded changes and corrected evidence record without closing those gates. Actual desktop Safari and representative iPad Safari interaction/performance/accessibility and complete memory evidence must still be supplied; failed budgets and indeterminate sustained foreground acceptance remain explicit. Stage 04 continuity/selection work is not implemented or authorized by this record.

## Independent champion review (2026-09-07)

The Driveability Champion reviewed implementation `b4ee195` against `5b12c38`, then reviewed correction `29bde319d43b550254bad92858e9cb452c3c8aeb`. The navigation/loading cleanup, passive instrumentation, independent cuboid-contact measurement, physical scenarios and browser evidence were examined. No additional runtime defect was identified within this review's scope.

The parent independently ran `npm run verify`: the five-cell offline corpus, **248 tests in 24 files**, static checks with **zero errors/warnings**, and production build passed. All three rebuilt asset identities match the recorded build. The all-five-cell physical matrix and source/occupancy/courtyard queries reproduced the retained JSON exactly, excluding timestamps. All **50 protected files** match the baseline, **148 media files** match their recorded byte counts/hashes, and local documentation links/anchors and diff checks passed. The parent visually inspected the completed phone portrait, tablet landscape and Firefox overhead-camera stills; no additional device session or full browser matrix rerun is claimed.

Review found displayed Chromium Trafalgar paused before its cycle-28 reset, contradicting an unqualified sustained-pass interpretation. Correction `29bde31` retains all **24 original evidence records** byte-for-byte, makes the retrospective acceptance limits explicit, and adds a latched foreground-state guard. The parent independently reran its **eight deterministic tests**, including the phase getter and pause/hidden/transient-state cases, and checked the runner integration and syntax. The four Chromium DOM-fixture checks are the implementation task's guard evidence, not parent reruns or new application benchmarks.

**Decision:** the bounded changes and corrected record are reviewed; Phase 04 and Stage 03 are **not complete**. Sustained acceptance needs valid foreground-interval evidence under the fixed budgets; the displayed Chromium lifecycle interruptions remain unresolved. Actual desktop Safari/iPad Safari checks and the remaining memory measurements or explicitly agreed measurement method are still required. Numeric timing results, headless lifecycle passes and JavaScript heap observations retain only their declared scope. Canonical main remains clean at `712fe6f`, and the Phase 03 source worktree remains clean at `5b12c38`; no canonical integration, push or deployment was performed.
